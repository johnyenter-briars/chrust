use super::boardstate::BoardState;

pub fn max_choice(board_state: BoardState, max_depth: i32) {
    let good_actions: Vec<BoardState> = Vec::new();

    // for action in board_state.board.

    // for 
}